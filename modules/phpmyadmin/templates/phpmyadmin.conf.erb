Alias /phpmyadmin <%= @target %>/<%= @file_name %>
<Directory <%= @target %>/<%= @file_name %>>
    Options FollowSymLinks
    DirectoryIndex index.php

    <RequireAny>
        Require local
        Require ip 10.0.1
        Require ip 10.0.2
        Require ip 192.168.1
    </RequireAny>

    <FilesMatch "\.php$">
        SetHandler proxy:unix:/var/run/php5-fpm.sock|fcgi://localhost
    </FilesMatch>

</Directory>

<Directory <%= @target %>/<%= @file_name %>/libraries>
    Order Deny,Allow
    Deny from All
</Directory>
<Directory <%= @target %>/<%= @file_name %>/setup/lib>
    Order Deny,Allow
    Deny from All
</Directory>
